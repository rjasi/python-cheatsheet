"""
Same as list but only contains 1 type
Like C arrays
Space efficient
"""

from array import array

def example():
    arr = array('f'. (1.0,2.0,3.0,4.0)) # need to provide type i.e 'f'
    arr # array('f', [1.0, 2.0, 3.0, 4.0])
